const rateLimit100 = {
    // test that it works when no setting provided, it should default to 100
    //ratelimit: 100,
};

const rateLimit500 = {
    ratelimit: 500,
};

const rateLimit1000 = {
    ratelimit: 1000,
};

export default { rateLimit100, rateLimit500, rateLimit1000 };
